*** Settings ***
Documentation     https://app.91guoxin.com/live-call-detail
Force Tags        prod
Resource          all_api_resource.txt

*** Test Cases ***
Live-call-detail
    [Documentation]    https://app.91guoxin.com/live-call-detail
    [Template]    Live-call-detail_Temp
    TestData01: 登录用户    ${EMPTY}    1    ${EMPTY}    ${AUTO_USERNAME}    ${AUTO_PASSWORD}    13937

Live-call-detail_negative
    [Documentation]    https://app.91guoxin.com/live-call-detail
    [Tags]    negative
    [Template]    Live-call-detail_negative_Temp

*** Keywords ***
Live-call-detail_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${id}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Live-call-detail    ${resp[4]}    ${id}
    Should Be Equal As Strings    ${resp[2]}    ${success}
    Should Be Equal As Strings    ${resp[1]}    ${code}
    Should Be Equal As Strings    ${resp[3]}    ${message}

Live-call-detail_negative_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${id}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Live-call-detail    ${resp[4]}    ${id}
    Should Not Be Equal As Strings    ${resp[2]}    ${success}
    Should Not Be Equal As Strings    ${resp[1]}    ${code}
    Should Not Be Equal As Strings    ${resp[3]}    ${message}
