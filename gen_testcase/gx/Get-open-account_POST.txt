*** Settings ***
Documentation     https://app.91guoxin.com/get-open-account
Force Tags        prod
Resource          all_api_resource.txt

*** Test Cases ***
Get-open-account
    [Documentation]    https://app.91guoxin.com/get-open-account
    [Template]    Get-open-account_Temp
    TestData01: 登录用户    ${EMPTY}    1    ${EMPTY}    ${AUTO_USERNAME}    ${AUTO_PASSWORD}    qiluce,tjpme

Get-open-account_negative
    [Documentation]    https://app.91guoxin.com/get-open-account
    [Tags]    negative
    [Template]    Get-open-account_negative_Temp

*** Keywords ***
Get-open-account_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${type}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Get-open-account    ${resp[4]}    ${type}
    Should Be Equal As Strings    ${resp[2]}    ${success}
    Should Be Equal As Strings    ${resp[1]}    ${code}
    Should Be Equal As Strings    ${resp[3]}    ${message}

Get-open-account_negative_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${type}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Get-open-account    ${resp[4]}    ${type}
    Should Not Be Equal As Strings    ${resp[2]}    ${success}
    Should Not Be Equal As Strings    ${resp[1]}    ${code}
    Should Not Be Equal As Strings    ${resp[3]}    ${message}
