*** Settings ***
Documentation     https://app.91guoxin.com/market/query/marketInfo
Force Tags        prod
Resource          all_api_resource.txt

*** Test Cases ***
Marketinfo
    [Documentation]    https://app.91guoxin.com/market/query/marketInfo
    [Template]    Marketinfo_Temp
    TestData01: 登录用户    ${EMPTY}    1    ${EMPTY}    ${AUTO_USERNAME}    ${AUTO_PASSWORD}    ios

Marketinfo_negative
    [Documentation]    https://app.91guoxin.com/market/query/marketInfo
    [Tags]    negative
    [Template]    Marketinfo_negative_Temp

*** Keywords ***
Marketinfo_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${platform}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Marketinfo    ${resp[4]}    ${platform}
    Should Be Equal As Strings    ${resp[2]}    ${success}
    Should Be Equal As Strings    ${resp[1]}    ${code}
    Should Be Equal As Strings    ${resp[3]}    ${message}

Marketinfo_negative_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${platform}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Marketinfo    ${resp[4]}    ${platform}
    Should Not Be Equal As Strings    ${resp[2]}    ${success}
    Should Not Be Equal As Strings    ${resp[1]}    ${code}
    Should Not Be Equal As Strings    ${resp[3]}    ${message}
