*** Settings ***
Documentation     https://app.91guoxin.com/market/query/timeline
Force Tags        prod
Resource          all_api_resource.txt

*** Test Cases ***
Timeline
    [Documentation]    https://app.91guoxin.com/market/query/timeline
    [Template]    Timeline_Temp
    TestData01: 登录用户    ${EMPTY}    1    ${EMPTY}    ${AUTO_USERNAME}    ${AUTO_PASSWORD}    xagusdv3
    ...    1

Timeline_negative
    [Documentation]    https://app.91guoxin.com/market/query/timeline
    [Tags]    negative
    [Template]    Timeline_negative_Temp

*** Keywords ***
Timeline_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${xcode}    ${period}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Timeline    ${resp[4]}    ${xcode}    ${period}
    Should Be Equal As Strings    ${resp[2]}    ${success}
    Should Be Equal As Strings    ${resp[1]}    ${code}
    Should Be Equal As Strings    ${resp[3]}    ${message}

Timeline_negative_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${code}    ${period}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Timeline    ${resp[4]}    ${code}    ${period}
    Should Not Be Equal As Strings    ${resp[2]}    ${success}
    Should Not Be Equal As Strings    ${resp[1]}    ${code}
    Should Not Be Equal As Strings    ${resp[3]}    ${message}
