*** Settings ***
Documentation     https://app.91guoxin.com/market/query/quotation
Force Tags        prod
Resource          all_api_resource.txt

*** Test Cases ***
Quotation
    [Documentation]    https://app.91guoxin.com/market/query/quotation
    [Template]    Quotation_Temp
    TestData01: 登录用户    ${EMPTY}    1    ${EMPTY}    ${AUTO_USERNAME}    ${AUTO_PASSWORD}    xagusdv3,cuv3

Quotation_negative
    [Documentation]    https://app.91guoxin.com/market/query/quotation
    [Tags]    negative
    [Template]    Quotation_negative_Temp

*** Keywords ***
Quotation_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${xcode}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Quotation    ${resp[4]}    ${xcode}
    Should Be Equal As Strings    ${resp[2]}    ${success}
    Should Be Equal As Strings    ${resp[1]}    ${code}
    Should Be Equal As Strings    ${resp[3]}    ${message}

Quotation_negative_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${code}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Quotation    ${resp[4]}    ${code}
    Should Not Be Equal As Strings    ${resp[2]}    ${success}
    Should Not Be Equal As Strings    ${resp[1]}    ${code}
    Should Not Be Equal As Strings    ${resp[3]}    ${message}
