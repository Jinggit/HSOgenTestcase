*** Settings ***
Documentation     https://app.91guoxin.com/set-receive-call-status
Force Tags        prod
Resource          all_api_resource.txt

*** Test Cases ***
Set-receive-call-status
    [Documentation]    https://app.91guoxin.com/set-receive-call-status
    [Template]    Set-receive-call-status_Temp
    TestData01: 登录用户    ${EMPTY}    1    ${EMPTY}    ${AUTO_USERNAME}    ${AUTO_PASSWORD}    1

Set-receive-call-status_negative
    [Documentation]    https://app.91guoxin.com/set-receive-call-status
    [Tags]    negative
    [Template]    Set-receive-call-status_negative_Temp

*** Keywords ***
Set-receive-call-status_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${isReceiveCall}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Set-receive-call-status    ${resp[4]}    ${isReceiveCall}
    Should Be Equal As Strings    ${resp[2]}    ${success}
    Should Be Equal As Strings    ${resp[1]}    ${code}
    Should Be Equal As Strings    ${resp[3]}    ${message}

Set-receive-call-status_negative_Temp
    [Arguments]    ${testdata_info}    ${code}    ${success}    ${message}    ${username}    ${password}
    ...    ${isReceiveCall}
    Log    ${testdata_info}
    ${resp}=    Login    ${username}    ${password}
    ${resp}=    Set-receive-call-status    ${resp[4]}    ${isReceiveCall}
    Should Not Be Equal As Strings    ${resp[2]}    ${success}
    Should Not Be Equal As Strings    ${resp[1]}    ${code}
    Should Not Be Equal As Strings    ${resp[3]}    ${message}
